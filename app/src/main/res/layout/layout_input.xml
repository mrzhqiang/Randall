<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
  <data>
    <variable
        name="input"
        type="cn.mrzhqiang.randall.viewmodel.InputViewModel"
        />
  </data>
  <android.support.design.widget.TextInputLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      app:counterEnabled="@{input.counterEnabled}"
      app:counterMaxLength="@{input.counterMaxLength}"
      app:error="@{input.error}"
      app:errorEnabled="@{input.errorEnabled}"
      app:passwordToggleEnabled="@{input.passwordToggleEnabled}"
      >
    <android.support.design.widget.TextInputEditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:digits="@{input.digits}"
        android:enabled="@{input.enabled}"
        android:hint="@{input.hint}"
        android:inputType="@{input.inputType}"
        android:text="@={input.text}"
        tools:hint="请输入"
        />
  </android.support.design.widget.TextInputLayout>
</layout>
